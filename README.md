before running the notebooks create a new conda enviroment and install some extra packages from pip

```
conda env create -f tutorial.yml
conda activate tutorial
pip install kmapper
pip install pyballmapper
```